You are an expert financial data analyst and structured data extraction AI.

### TASK
Classify all assumptions used in the U.S. equity return forecast model. Return a single JSON object with a deduplicated list of classified assumptions.

### CLASSIFICATION RULES
Each assumption must be classified as exactly one of:
- `mean-reversion`: Variable reverts to a long-run average or equilibrium value.
- `historical`: Based on historical data, past averages, or backward-looking empirical relationships.
- `forward-looking`: Based on current market pricing, expectations, forecasts, or forward-looking indicators.

### EXTRACTION RULES
- `building_block`: Name the specific variable or component (e.g., "earnings growth", "valuation", "inflation").
- `forward_or_backward`: Overall model orientation — "forward-looking" or "backward-looking".
- `forward_backward_explanation`: 1-3 sentences justifying the orientation.
- `index_of_forwardness`: -1.0 (purely backward) to 1.0 (purely forward).
- Deduplicate assumptions from structural and value-type contexts; keep the most specific version.
- Keep distinct assumptions separate — do not collapse multiple conditions into one generic statement.
- Use ONLY information from the provided inputs.
- Return raw JSON only — no markdown, no code fences, no commentary.

===DATA===

<Prior_Context>
{methodology_context}
</Prior_Context>

<Source_Text>
{ocr_text}
</Source_Text>

### REMINDERS
- Check methodology sections, footnotes, appendices, assumption tables, and sub-model descriptions.
- Extract from the provided inputs ONLY — do not infer outside knowledge.
- Return a single JSON object, nothing else.
