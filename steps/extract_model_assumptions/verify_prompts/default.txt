Task: You are given a draft classification of assumptions for a U.S. equity return forecast model, produced by merging multiple preliminary extractions. Verify each item against the original document and prior context.

Draft extraction (merged from multiple passes):
{draft_result}

Instructions:
1. Review each assumption — keep only those grounded in the document or prior context. Remove any that are fabricated or unsupported.
2. Verify each classification label is correct:
   - "mean-reversion": variable reverts to a long-run average or equilibrium value
   - "historical": based on historical data, past averages, or backward-looking empirical relationships
   - "forward-looking": based on current market pricing, expectations, forecasts, or forward-looking indicators
3. Ensure each `building_block` reference is specific and accurate — name the actual variable or component.
4. Remove duplicates or near-duplicates, keeping the more specific or faithful wording.
5. Verify `techniques_used` — each technique must be grounded in the text. Verify complexity scores against this scale:
   - 1-2: Simple historical averages, fixed assumptions, naive extrapolation
   - 3-4: Trend extrapolation, basic regression, moving averages, rule-of-thumb adjustments
   - 5-6: Multi-factor regression, mean-reversion models, Gordon growth model, dividend discount models, building-block approaches
   - 7-8: Monte Carlo simulation, VAR/cointegration, Bayesian estimation, regime-switching models, factor-based risk premia decomposition
   - 9-10: DSGE, machine learning / AI-driven forecasts, Kalman filtering, option-pricing-based approaches, proprietary simulation engines
6. Add any techniques present in the text but missing from the draft.
7. Verify `sophistication_index` is consistent with the techniques listed and their centrality to the model.
8. Ensure `sophistication_explanation` is concise (2-3 sentences) and justified by the report content.

Return exactly one JSON object with the same shape:
{
  "assumptions": [
    {
      "assumption": "string",
      "building_block": "string",
      "classification": "mean-reversion | historical | forward-looking"
    }
  ],
  "techniques_used": [
    {
      "technique_name": "string",
      "complexity": 5
    }
  ],
  "sophistication_index": 5,
  "sophistication_explanation": "string"
}

- Use ONLY information from the report and the provided context. Do not infer or import outside knowledge.
- Return JSON only. No markdown, no explanation, no code fences.

{methodology_context}
OCR TEXT:
{ocr_text}
