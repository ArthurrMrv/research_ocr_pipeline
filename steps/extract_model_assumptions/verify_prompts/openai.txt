You are an expert financial model auditor.

### TASK
Verify and finalize a draft classification of assumptions for a U.S. equity return forecast model. The draft was merged from multiple preliminary passes. Clean, verify against source text, and augment with any missed items.

### VERIFICATION STEPS

1. **Grounding**: Keep only assumptions explicitly supported by the source text or context. Remove fabricated or unsupported items.
2. **Classification**: Verify each label is strictly one of:
   - `mean-reversion`: variable reverts to long-run average or equilibrium.
   - `historical`: based on historical data, past averages, backward-looking relationships.
   - `forward-looking`: based on current pricing, expectations, forecasts, forward indicators.
3. **Building blocks**: Ensure each `building_block` names the actual variable or component.
4. **Deduplication**: Remove duplicates/near-duplicates, keep the most specific wording. Do not collapse distinct conditions into generic statements.
5. **Rescan**: After filtering, do one full rescan of the source text for supported assumptions the draft missed. Check methodology sections, footnotes, appendices, assumption tables, sub-model descriptions.
6. **Global metrics**: Verify `forward_or_backward` and `index_of_forwardness` are consistent with the finalized assumption list. Write a concise `forward_backward_explanation` (1-3 sentences).

### RULES
- Use ONLY information from the provided inputs. Do not infer outside knowledge.
- Return raw JSON only — no markdown, no code fences, no commentary.

===DATA===

### DRAFT
{draft_result}

<Context>
{methodology_context}
</Context>

<Source_Text>
{ocr_text}
</Source_Text>

### REMINDERS
- Keep distinct assumptions separate — do not merge different conditions.
- Classification must be exactly one of: mean-reversion, historical, forward-looking.
- Return a single JSON object, nothing else.
