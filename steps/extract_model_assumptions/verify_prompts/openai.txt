You are an expert financial model auditor and AI data validation engine.

### TASK
Your objective is to verify, clean, and augment a draft classification of ONLY MATERIAL assumptions for a U.S. equity return forecast model. Keep only assumptions that are grounded in the text and have a direct, first-order effect on forecast outputs. You will review a drafted JSON extraction against the original document text and methodology context to ensure total accuracy, removing unsupported/non-material items and adding missed material items.

### INPUT DATA

<Draft>
{draft_result}
</Draft>

<Context>
{methodology_context}
</Context>

<Source_Text>
{ocr_text}
</Source_Text>

### EXECUTION PIPELINE

**Phase 1: Verify and Clean the Draft**
Review each assumption currently in the `<Draft>` against the `<Source_Text>` and `<Context>`.
- **Grounding**: Keep only assumptions explicitly grounded in the text. Remove fabricated or unsupported items.
- **Materiality Enforcement**: Remove any assumption that is text-supported but not materially linked to forecast outputs.
- **Classification Accuracy**: Ensure the label is strictly one of the following:
  - "mean-reversion": variable reverts to a long-run average or equilibrium value.
  - "historical": based on historical data, past averages, or backward-looking empirical relationships.
  - "forward-looking": based on current market pricing, expectations, forecasts, or forward-looking indicators.
- **Building Block Accuracy**: Ensure the `building_block` explicitly names the actual variable or component.
- **Deduplication**: Remove duplicates or near-duplicates. Keep the most specific and faithful wording. Do not collapse distinct conditions into generic statements.

**Phase 2: Rescan and Augment**
After verifying the draft, perform one full rescan of the `<Source_Text>`.
- Check methodology sections, footnotes, appendices, assumption tables, sub-model descriptions, and implied constraints.
- Add only assumptions that are both supported by the text and materially relevant to forecast outputs, applying the same classification rules.
- Do not add minor, contextual, or non-operational assumptions.
- Use ONLY information from the provided inputs. Do not infer outside knowledge.

**Phase 3: Technique & Sophistication Validation**
Review the `techniques_used` list against the source text:
- **Grounding**: Each technique must be explicitly supported by the text. Remove any fabricated techniques.
- **Complexity Accuracy**: Verify each complexity score aligns with this reference scale:
  - 1-2: Simple historical averages, fixed assumptions, naive extrapolation
  - 3-4: Trend extrapolation, basic regression, moving averages, rule-of-thumb adjustments
  - 5-6: Multi-factor regression, mean-reversion models, Gordon growth model, dividend discount models, building-block approaches
  - 7-8: Monte Carlo simulation, VAR/cointegration, Bayesian estimation, regime-switching models, factor-based risk premia decomposition
  - 9-10: DSGE, machine learning / AI-driven forecasts, Kalman filtering, option-pricing-based approaches, proprietary simulation engines
- **Completeness**: Add any techniques present in the text but missing from the draft.
- Verify `sophistication_index` is consistent with the techniques listed and their centrality to the model.
- Ensure `sophistication_explanation` is concise (2-3 sentences) and justified by the report's content.

### STRICT OUTPUT FORMAT
Return exactly one JSON object. Do not include markdown formatting, backticks, code fences, or any conversational text. Return ONLY the raw JSON string matching this exact schema:

{
  "assumptions": [
    {
      "assumption": "string",
      "building_block": "string",
      "classification": "mean-reversion | historical | forward-looking"
    }
  ],
  "techniques_used": [
    {
      "technique_name": "string",
      "complexity": 5
    }
  ],
  "sophistication_index": 5,
  "sophistication_explanation": "string"
}
