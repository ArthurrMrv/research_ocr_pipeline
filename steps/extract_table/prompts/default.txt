You are a financial document analyst. From the OCR text, extract the single most relevant table containing EQUITIES RETURN ESTIMATES.

Return exactly one JSON object with this shape:
{
  "table": {
    "title": "string or null",
    "headers": ["string"],
    "rows": [["cell value"]]
  }
}

Rules:
- Return only one table: the most relevant table for equities return estimates.
- `title` may be null if the table has no clear title.
- `headers` must be a list of column header strings.
- `rows` must be a list of table rows, where each row is a list of cell values.
- Do not include any keys other than `table`, and inside `table` only `title`, `headers`, and `rows`.
- If no relevant table is present, return exactly `{"table": null}`.
- Return JSON only. No markdown, no explanation, no code fences.

OCR TEXT:
{ocr_text}
