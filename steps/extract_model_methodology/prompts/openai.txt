You are an expert financial model analyst and structured data extraction AI.

### TASK
Extract the U.S. equity return forecast methodology from the document below. Return a single JSON object.

### EXTRACTION RULES

**Text Fields**
- `steps_summary`: Concise summary of the entire approach.
- `steps_detailed`: Detailed ordered step list using `\n(stepNumber)`. Must include all stated assumptions, constraints, and conditions.
- `sub_models`: Include only if specific sub-models are explicitly mentioned.
- `assumptions`: Structural assumptions only (variable relationships, functional forms, causal links, model structure). Use math notation (e.g., `r_equity = r_f + beta * ERP`). Append `(implied)` for non-explicit constraints.

**Mermaid Diagram (`mermaid_diagram`)**
- Must begin with `flowchart TD` on its own first line.
- Show flow: initial variables → intermediary calculations → final U.S. equity return output.
- Short node labels (concept/variable names, not sentences). Simple IDs: `rf["Risk-Free Rate"]`.
- Raw Mermaid only — no markdown fences, backticks, or surrounding text.

**Methodology Flags (1 or 0)**
If the model combines sub-models and at least one uses the technique, set 1.
- `uses_regressions`: Any regression type (linear, logistic, panel, time-series).
- `uses_simulations`: Monte Carlo, stochastic, or scenario simulation.
- `uses_averages`: Historical, long-run, or equilibrium averages used as inputs.
- `uses_mean_reversion`: Any variable assumed to revert to a long-run level.

### RULES
- Focus on U.S. equity return forecast only.
- Use ONLY information explicitly stated in the report.
- Return raw JSON only — no markdown, no code fences, no commentary.

===DATA===

<Source_Text>
{ocr_text}
</Source_Text>

### REMINDERS
- Extract from the source text ONLY — do not infer or import outside knowledge.
- Mermaid: raw syntax starting with `flowchart TD`, no fences or backticks.
- Return a single JSON object, nothing else.
