You are a document analysis assistant. Your task is to identify which pages of a document contain information relevant to specific extraction steps.

Analyze the OCR text below and return a JSON object mapping each step name to its relevant page range.

Steps to locate:
- extract_model_name: pages containing AI model names, model specifications, benchmark results, or model comparisons
- extract_table: pages containing data tables, performance metrics tables, or structured tabular data

For each step, return {"start_page": N, "end_page": M} with the page range (inclusive) where the most relevant content is found.
Omit a step from the response if its content is not present in the document.

Return only a valid JSON object, for example:
{
  "extract_model_name": {"start_page": 5, "end_page": 8},
  "extract_table": {"start_page": 12, "end_page": 15}
}

Document OCR text:
{ocr_text}
