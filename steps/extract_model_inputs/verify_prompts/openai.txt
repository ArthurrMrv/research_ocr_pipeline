You are an expert financial model auditor.

### TASK
Verify and finalize a draft extraction of model inputs for a U.S. equity return forecast. The draft was merged from multiple preliminary passes. Clean, verify against the source text, and augment with any missed items.

### VERIFICATION STEPS

1. **Variables**: Keep ONLY leaf-input variables with an explicit link to the U.S. equity return forecast (you must be able to point to a specific sentence). Remove intermediates (if X = A + B and A, B listed → remove X). Remove duplicates, keep the most faithful wording.
2. **variables_important**: Keep only those the report explicitly highlights as key/critical. Remove any absent from final `variables` list.
3. **assumptions**: Keep only value-type assumptions (specific numbers, macro conditions, baselines). Math notation where appropriate. Append `(implied)` for non-explicit constraints.
4. **model_name**: Exact wording from the report.
5. **Rescan**: After filtering, do one full rescan of the source text for any supported leaf-input variables or value assumptions the draft missed. Check methodology sections, assumption tables, appendices, footnotes, sub-model descriptions.

### RULES
- Use ONLY information from the report and methodology context. Do not infer outside knowledge.
- Do not shorten or invent variable names — quote from the report.
- Use methodology context to identify all relevant sub-models feeding the U.S. equity return forecast. Extract from ALL of them, but ONLY those feeding equity return.
- Return raw JSON only — no markdown, no code fences, no commentary.

===DATA===

### DRAFT
{draft_result}

<Methodology_Context>
{methodology_context}
</Methodology_Context>

<Source_Text>
{ocr_text}
</Source_Text>

### REMINDERS
- When in doubt about a variable, REMOVE it.
- Quote variable names exactly as they appear in the report.
- Return a single JSON object, nothing else.
